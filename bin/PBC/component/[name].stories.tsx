import { ReactElement } from 'react';
import { name } from './[name]';

export default {
    title: 'Piggy Bank Crew/[name with space]',
};

export const _name = (): ReactElement => {
    return <name />;
};
