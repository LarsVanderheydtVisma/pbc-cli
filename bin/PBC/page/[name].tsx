import { PageData } from '@Component/PageLoader';
import { name } from './components/name';

export const page: PageData = {
    page: name,
    modules: [
        {
            id: '[name]',
        },
    ],
};
